print type_name(p$ip6$exts)

record { id:count; 
	hopopts:record { nxt:count; len:count; 
				options:vector of record { otype:count; len:count; data:string; }; 
			}; 
	dstopts:record { nxt:count; len:count; 
				options:vector of record { otype:count; len:count; data:string; }; 
		       }; 
	routing:record { nxt:count; len:count; rtype:count; segleft:count; data:string; }; 
	fragment:record { nxt:count; rsv1:count; offset:count; rsv2:count; more:bool; id:count; }; 
	ah:record { nxt:count; len:count; rsv:count; spi:count; seq:count; data:string; }; 
	esp:record { spi:count; seq:count; }; 
	mobility:record { nxt:count; len:count; mh_type:count; rsv:count; chksum:count; 
				msg:record { id:count; 
							brr:record { rsv:count; 
									options:vector of record { otype:count; len:count; data:string; }; 
					   			   };
							hoti:record { rsv:count; cookie:count; 
									options:vector of record { otype:count; len:count; data:string; }; 
					    			    }; 
							coti:record { rsv:count; cookie:count; 
									options:vector of record { otype:count; len:count; data:string; }; 
					    			    }; 
							hot:record { nonce_idx:count; cookie:count; token:count; 
									options:vector of record { otype:count; len:count; data:string; }; 
					   			   }; 
			        			cot:record { nonce_idx:count; cookie:count; token:count; 
									options:vector of record { otype:count; len:count; data:string; }; 
					   			   }; 
							bu:record { seq:count; a:bool; h:bool; l:bool; k:bool; life:count; 
									options:vector of record { otype:count; len:count; data:string; }; 
					  			  }; 
							back:record { status:count; k:bool; seq:count; life:count; 
									options:vector of record { otype:count; len:count; data:string; }; 
					    			    }; 
							be:record { status:count; hoa:addr; 
									options:vector of record { otype:count; len:count; data:string; }; 
				          			  }; 
					   }; 
			}; 
	}
